# 🏋️‍♂️ Weight Conversion Program (Java)

A simple **Java console application** that converts weight between **pounds (lbs)** and **kilograms (kgs)**.  
This program demonstrates how to use **user input**, **conditional statements**, and **basic arithmetic operations** in Java.

---

## 🚀 Features
- Convert **lbs → kgs**
- Convert **kgs → lbs**
- Handles invalid menu options
- Uses `Scanner` for user input
- Nicely formatted output using `printf`

---

## 🧠 Concepts Practiced
- Taking user input with `Scanner`
- Using `if-else` control statements
- Performing mathematical operations
- Formatting decimal output (`%.2f`)
- Resource management (`scanner.close()`)